<?xml version="1.0"?>
<project
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
        xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.annaru</groupId>
        <artifactId>annaru-queue</artifactId>
        <version>1.0.0</version>
    </parent>

    <groupId>com.annaru</groupId>
    <artifactId>queue-service</artifactId>
    <version>${queue-service.version}</version>
    <packaging>jar</packaging>

    <name>queue-service</name>
    <url>http://maven.apache.org</url>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.annaru</groupId>
            <artifactId>queue-common</artifactId>
            <version>${queue-common.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-quartz</artifactId>
        </dependency>
    </dependencies>
</project>
